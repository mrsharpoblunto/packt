/* @flow */
import twgl from 'twgl.js';

export default class TerrainComponent {
  constructor() {}
  // a heightmap. Will eventually be separated into chunks
  // of varying LODS. For now will be a flat 2d plane
  // while we get the physics right
}
